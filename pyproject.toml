[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "tal-qual"
version = "0.1.0"
description = "Spark/PySpark MVP for extracting Portuguese comparison candidates."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
